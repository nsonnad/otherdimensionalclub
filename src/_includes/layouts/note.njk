---
layout: default
---
<div class="note-content">
  <h1>{{ title }}</h1>
  <p class="note-dateline">– {{ page.date | readableDate }} </p>
  <div class="note-category-tags">
    {% for category in categories %}
        <div class="note-category-tag category-{{ category }}">
          <a href="/{{ category | slug }}/">{{ category }}</a>
        </div>
    {% endfor %}
  </div>

  {{ content | safe }}
</div>
