<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/assets/main.css">
</head>
<body>
  <header>
    <h1>planet m</h1>
  </header>
  <main>
    <nav>
      <ul class="category-nav">
        <li class="category-all"><a href="/notes/">all</a></li>
        {% for category in collections.categoryList %}
          <li class="category-{{ category }}"><a href="/{{ category | slug }}/">{{ category }}</a></li>
        {% endfor %}
      </ul>
    </nav>

    {{ content | safe }}
  </main>
</body>
</html>
